require "spec_helper"

describe WhiteBoxTestingExamples do
  it "has a version number" do
    expect(WhiteBoxTestingExamples::VERSION).not_to be nil
  end

  it "the truth" do
    expect(true).to be_truthy
  end
end
